# Availability Inventory Map (Task #11, фаза 1)

**Правило фази:** нічого не змінювати. Тільки знайти, описати й довести числами.

**Джерело доказів:** production-знімок `farforre_rentalhub.sql`
(SHA-256 `506a79c7258dde6dbd7f2f8cd33cc6b10a49f252198aa815e055f2c34d4e752b`),
відновлений локально у схему `prod_snapshot` (MySQL 5.7.44).
Живої production-БД не торкалися. Усі запити — `SELECT`.

Період для порівняння формул: `2026-09-06 .. 2026-09-13`. Перевірено 8173 активних товари (`products.status = 1`).

---

## 1. Головний висновок

Єдиного джерела правди немає. Доступність рахується **в 11 місцях за 9 різними формулами**.
Це не стильова розбіжність — формули дають **різні числа на тих самих даних**.

| Порівняння | Товарів з різним результатом | Макс. розбіжність (шт) |
|---|---|---|
| `availability_checker` ↔ каталог (список) | **294** | **210** |
| `availability_checker` ↔ event-tool (каталог) | **429** | **210** |
| `availability_checker` ↔ inventory (заморозка) | **588** | — |
| `availability_checker` ↔ issue_cards | **60** | — |
| каталог ↔ event-tool | **162** | — |

Тобто на один і той самий товар Website, Admin, Calendar і Sets можуть показати
розбіжність **до 210 одиниць**.

---

## 2. Реєстр точок розрахунку

Легенда компонентів: `T` = `products.quantity`, `R` = резерв із замовлень,
`IR` = в оренді, `FQ` = `products.frozen_quantity`, `PDH` = `product_damage_history`,
`SR` = `event_soft_reservations`, `PR` = `product_reservations`.

| # | Місце | Формула | Фільтр дат | Статуси, що резервують |
|---|---|---|---|---|
| A | `utils/availability_checker.py` → `check_product_availability` | `T − R` | ✅ перетин | `processing, ready_for_issue, issued, on_rent` |
| B | `routes/catalog.py` → список/`items-by-category` | `T − R − IR − PDH` | ✅ / ❌ (дві гілки) | `processing, ready_for_issue, awaiting_customer, pending` + окремо `issued, on_rent` |
| C | `routes/event_tool.py` → каталог товарів | `T − FQ − IR − R` | ✅ / ❌ (дві гілки) | ті ж, що B |
| D | `routes/event_tool.py` → `POST /products/check-availability` | `T − FQ − R − SR` | ✅ перетин | `processing, ready_for_issue, issued, on_rent` |
| E | `routes/product_reservations.py` → `check_product_availability` | `T − PR` | ✅ перетин | таблиця `product_reservations` |
| F | `routes/issue_cards.py` | `T − R` | ❌ **немає** | `processing, ready_for_issue, issued, on_rent` + `is_archived = 0` |
| G | `routes/inventory.py` → заморозка | `T − FQ` | ❌ немає | — (не дивиться на замовлення) |
| H | `routes/event_tool_integration.py` → створення замовлення | `T − PR` | ✅ перетин | `product_reservations` |
| I | `routes/catalog.py` → `GET /check-availability/{sku}` | `T > 0` | ❌ **ігнорує дати** | — (жодних) |
| J | `routes/order_modifications.py` | `available = p.quantity` | ❌ немає | — (жодних) |
| K | `routes/orders.py` → позиції замовлення | `available = p.quantity`, `reserved = 0` | ❌ немає | — (`TODO` у коді) |
| L | `routes/product_sets.py` (Sets/Bundles) | **немає розрахунку** | ❌ | — |

Викликають спільне ядро (A) лише **2 місця** з 11: `orders.py` `POST /check-availability`
і перевірка перед переходом у `processing`. Решта — власні SQL-запити.

---

## 3. Доведені дефекти (числа з production-знімка)

### 3.1 Статус `partial_return` невидимий для ВСІХ формул
```
partial_return: 1 замовлення, 55 одиниць товару
```
Цього статусу немає ні в одному списку резервування. Товар фізично у клієнта,
але всі 11 точок вважають його вільним.

### 3.2 `awaiting_customer` / `pending` трактуються протилежно
```
awaiting_customer: 2 замовлення, 76 одиниць
ready_for_issue:   1 замовлення, 55 одиниць
```
`availability_checker` (A) **не** резервує `awaiting_customer`, а каталог (B, C) — **резервує**.
Одна й та сама позиція одночасно «вільна» в Admin і «зайнята» в каталозі.

### 3.3 `is_archived` фільтрується лише в одному місці
```
активні замовлення з is_archived = 1: 1 замовлення, 55 одиниць
```
`issue_cards` (F) їх виключає, `availability_checker` (A) — ні. Звідси 60 розбіжностей.

### 3.4 Два незалежні джерела «на обробці» розходяться
```
frozen_quantity > 0, але PDH порожній: 145 товарів
PDH > 0, але frozen_quantity = 0:        3 товари
будь-яка невідповідність:              181 товар
```
`event_tool` (C, D) і `inventory` (G) вірять `products.frozen_quantity`,
каталог (B) і `warehouse` — `product_damage_history`. Обидва вважаються правдою.

### 3.5 `written_off` товар досі «доступний»
```
state = 'written_off': 11 товарів, 389 одиниць, frozen лише 10
```
Жодна формула не виключає списаний товар. ~379 одиниць списаного товару
можна забронювати.

### 3.6 Три таблиці, на яких тримається логіка, у production ВІДСУТНІ
```
product_reservations  → MISSING
product_sets          → MISSING
product_set_items     → MISSING
```
Наслідки:
- **E** (`product_reservations` check) — непрацездатний;
- **H** (створення замовлення з event-tool) — `COALESCE(SUM(pr.quantity), 0)` дає **0**,
  тобто перевірка конфліктів **завжди проходить**, навіть коли товару немає;
- **L** (Sets/Bundles) — усі запити звертаються до відсутніх таблиць.

`product_reservations` створюється лише вручну через `POST /migrate` — це не міграція,
а endpoint. У `000_baseline.sql` цих таблиць немає.

### 3.7 Frontend передає дати в endpoint, який їх ігнорує
`frontend/src/api/client.ts:119` надсилає `fromDate` / `toDate` у
`GET /catalog/check-availability/{sku}`, а endpoint (I) відповідає просто `quantity > 0`.
Дати відкидаються молча — UI показує «доступно» без перевірки періоду.

### 3.8 Soft reservations враховує лише 1 точка з 11
`event_soft_reservations` читає **тільки** D. Мудборд може «тримати» товар,
і жодна інша частина системи цього не бачить.

### 3.9 Sets/Bundles не мають availability взагалі
`product_sets.py` повертає `p.quantity as stock` — сирий залишок,
без резервів, без дат, без обробки. Доступність набору = не обчислюється ніде.

---

## 4. Цільова архітектура (ще НЕ реалізована)

```
Website ─┐
Admin ───┤
Orders ──┼──→ AvailabilityService ──→ Inventory / DB
Calendar ┤
Warehouse┤
Sets ────┘
```

Кандидат на ядро — `utils/availability_checker.py`: це єдине місце, де вже є
перетин дат, `exclude_order_id`, попередження про часткові повернення й обробку.
Але його формула (`T − R`) — **не** найповніша: вона не віднімає обробку
й не фільтрує `is_archived`.

---

## 5. Блокер фази 2

Вимога «не змінювати поведінку» **неможлива для всіх точок одночасно**:
формули суперечать одна одній на 294–588 товарах. Уніфікація за визначенням
змінить результат щонайменше для 8 із 11 точок.

Тому вибір канонічної семантики — **продуктове рішення, не технічне**.
Потрібне рішення власника щодо:

1. чи резервує `awaiting_customer` / `pending` (розбіжність A ↔ B);
2. чи резервує `partial_return` (зараз — ніде, 55 одиниць);
3. джерело правди для «на обробці»: `frozen_quantity` чи `product_damage_history` (181 розбіжність);
4. чи виключати `written_off` (389 одиниць);
5. чи враховувати `event_soft_reservations` глобально;
6. чи фільтрувати `is_archived`;
7. доля відсутніх таблиць `product_reservations` / `product_sets`.

До отримання цих рішень фаза 2 не починається.

---

## 7. Рішення власника (2026-09-06)

| # | Питання | Рішення | Наслідок для сервісу |
|---|---|---|---|
| 1 | `awaiting_customer` / `pending` резервують? | **Так, резервують** | Формула A приводиться до поведінки каталогу; перевірка перекриття дат обов'язкова |
| 2 | `partial_return` резервує неповернений залишок? | **Так** — фактично заморожений товар (прострочений, повернення не гарантоване) | Статус входить у список резервування; закриває дірку на 55 одиниць |
| 3 | Джерело правди «на обробці» | **`products.frozen_quantity`** | PDH більше не впливає на доступність (лишається журналом); знімає 181 розбіжність |
| 5 | Soft reservations (мудборд) | **Враховувати глобально**, включно з клієнтським замовленням | `event_soft_reservations` входить у канонічну формулу для всіх точок |
| 7 | Відсутні таблиці `product_reservations` / `product_sets` | **Міграцію зараз не додавати**, логіку дописувати згодом | Точки E, H, L не переводяться на сервіс; H лишається відомим дефектом |
| 4 | `written_off` виключати з доступних? | **НЕ виключати** (рішення за доказами, див. §7.4) | `products.state` не впливає на доступність; окремо зафіксовано як дефект даних |
| 6 | `is_archived` виключати з резерву? | **Так, виключати** — архів = повністю закритий ордер (формулювання власника) | `is_archived = 0` входить у канонічну формулу |

### 7.1 Канонічна формула (проєкт за отриманими рішеннями)

```
available(product, from, to) =
    products.quantity
  - products.frozen_quantity                  -- рішення 3
  - SUM(order_items.quantity)                 -- рішення 1 + 2
      по замовленнях зі статусами
      {processing, ready_for_issue, awaiting_customer, issued, partial_return}
      з перетином періоду [from, to]
  - SUM(event_soft_reservations.quantity)     -- рішення 5
      (активні, не протерміновані, з перетином періоду)
```

### 7.2 Знахідка при перевірці статусів (питання 1)

Власник просив перевірити, чи статуси не суперечать між сайтом і адмінкою. Перевірено по фактичних даних.

Реальні статуси в БД — рівно 8:

| Статус | Записів | З датою в майбутньому | Резервує за рішенням |
|---|---|---|---|
| `completed` | 692 | 0 | ні |
| `cancelled` | 169 | 9 | ні |
| `returned` | 84 | 2 | ні |
| `issued` | 29 | 29 | **так** |
| `processing` | 18 | 18 | **так** |
| `awaiting_customer` | 2 | 2 | **так** |
| `ready_for_issue` | 1 | 0 | **так** |
| `partial_return` | 1 | 0 | **так** |

**КРИТИЧНО:** статуси, за якими код резервує товар, у базі не існують ніде:

```
on_rent   -> 0 записів
pending   -> 0 записів
confirmed -> 0 записів
new       -> 0 записів
draft     -> 0 записів
```

`on_rent` фігурує як «в оренді» майже в кожній формулі (A, B, C, D, F), але реальний
статус видачі — `issued`. `pending` присутній у формулі каталогу — його теж не існує.
Тобто частина перевірок резервувала товар за статусами-привидами; це і є причина
розбіжностей між сайтом і адмінкою. У канонічну формулу входять **лише фактично
існуючі** статуси, фантомні прибираються.

### 7.4 Питання 4: чому `written_off` НЕ виключається (рішення за доказами)

Власник відповів «не знаю, що робить written_off». Замість здогадки з'ясовано по коду й даних.

**Як стан виставляється.** `routes/damage_cases.py:137`:

```sql
UPDATE products
SET quantity = quantity - :qty,
    state = 'written_off'
WHERE product_id = :pid
```

Тобто при повній втраті кількість **уже віднімається** зі складу, а `state` виставляється
як позначка останньої події. Те саме в `product_damage_history.py:224` (`is_total_loss`)
і в `partial_returns.py` / `return_versions.py` (`processing_status='written_off'`).

**Наслідок:** `state='written_off'` означає «останньою подією по цій позиції було списання»,
а не «вся позиція списана». Залишок у `products.quantity` — це вже актуальний, **не** списаний товар.

**Підтвердження даними.** Списані позиції живі й у обороті прямо зараз:

| Замовлення | Статус | Період | Товар | Взято | Залишок |
|---|---|---|---|---|---|
| 8078 | `issued` | 2026-09-03 … 09-18 | Гортензія біла голова | 179 | 179 |
| 8253 | `issued` | 2026-09-04 … 09-07 | Моно ваза 15 см | 64 | 64 |
| 8106 | `processing` | 2026-09-11 … 09-14 | Тарілка Harmony 21 см | 24 | 25 |

**Рішення: НЕ виключати.** Якби доступність обнулялася по `state='written_off'`,
267 одиниць у 3 працюючих замовленнях моментально стали б «недоступними» —
зламалися б реальні видачі. Реальне списання вже враховане зменшенням `quantity`,
тому додаткове віднімання було б подвійним.

`products.state` **не входить** у канонічну формулу. Окремо зафіксовано як
**дефект даних, а не логіки**: стан не повертається в `available` після обробки
(4 позиції зі `state='written_off'` взагалі не мають записів у журналі пошкоджень).
Це предмет окремої задачі очищення даних, не availability.

### 7.3 Уточнення по питаннях 4 і 6 (виміряно)

`written_off` — 11 позицій, 389 одиниць, усі видимі в каталозі (`status=1`);
найбільші: «Гортензія біла голова» 179 шт, «Моно ваза 15 см» 64 шт, «Колба 30 см» 33 шт.
Прямо зараз **267 одиниць** списаного товару стоять у **3 активних замовленнях**.

`is_archived=1` — 194 замовлення: 168 `cancelled`, 25 `completed` (товар не тримають)
і **1 у статусі `ready_for_issue`**, тобто формально активне.
`partial_return` (order 7361) і архівне `ready_for_issue` (order 7970) — **різні** записи,
тому рішення 2 і 6 не перекриваються.

---

## 8. Фаза 2: AvailabilityService (виконано)

### 8.1 Що створено

| Файл | Роль |
|---|---|
| `backend/services/availability/rules.py` | канонічні правила як константи — єдине місце, де живуть продуктові рішення |
| `backend/services/availability/service.py` | `AvailabilityService`: `get_availability`, `get_order_availability`, `get_bulk_availability` |
| `backend/services/availability/__init__.py` | публічний контракт пакета |
| `backend/utils/availability_checker.py` | переписано як **фасад** — формулу більше не дублює, лише додає попередження |
| `backend/tests/test_availability_service.py` | 27 регресійних тестів на кожне рішення власника |

Сервіс **read-only** (як `FinanceService` із Завдання №6): перевірено автоматично —
0 мутуючих SQL-конструкцій, 0 write-методів. DDL не виконується, схема не змінюється.

### 8.2 Виміряний вплив на production-знімку

Вікно 2026-09-06 … 2026-09-20, 8200 товарів, стара формула проти канонічної:

| Показник | Значення |
|---|---|
| Змінили доступність | **382 товари** |
| Стало МЕНШЕ (був овербукінг) | **382** |
| Стало БІЛЬШЕ (був недопродаж) | **0** |
| Максимальний розрив | **210 одиниць** на одному товарі |
| Сумарний розрив | **1456 одиниць** |

**Головний висновок:** усі 382 зміни — вниз, жодної вгору. Це означає, що стара
логіка **системно показувала товар, якого фізично немає**. Тобто розбіжність була
не «косметичною неточністю цифри», а джерелом реального овербукінгу: каталог
дозволяв продати 210 одиниць гілок фікуса, які в цей момент лежали на обробці.

Внесок кожного рішення окремо:

| Джерело зміни | Товарів | Одиниць |
|---|---|---|
| Рішення 3: `frozen_quantity` віднімається | 375 | 1582 |
| Рішення 1+2: `awaiting_customer` / `partial_return` | 23 | 76 |
| Рішення 6: архівні більше не резервують | 1 | 55 |
| Відмовлені позиції (`refused`) більше не тримають | 7 | 15 |
| Прибрано фантомні `on_rent` / `pending` | 0 | 0 |

Топ випадків овербукінгу: «Гілка фікуса 60 см» 328 → 118, «Колба 15 см» 78 → 27,
«Подушка кругла» 47 → 1, «Серветка 50×50» 71 → 27.

### 8.3 Знайдено при вимірі: soft-резерви мертві

Усі **56 записів** `event_soft_reservations` протерміновані: найпізніший
`expires_at` — 2026-04-12, тобто ~5 місяців тому. Тому рішення 5 зараз не
змінює жодної цифри, але **правило вже вбудоване** — як тільки мудборд почне
створювати свіжі резерви, вони врахуються глобально, а не в одній точці з 11.

Прибирання фантомних статусів теж дало 0 одиниць — і це очікувано: `on_rent`
і `pending` не мали рядків. Небезпека тут була не в поточних даних, а в тому,
що формули **перевіряли неіснуючі статуси замість справжніх**.

### 8.4 Що свідомо НЕ зроблено

- Точки E, H, L (`product_reservations`, `product_sets`) на сервіс не переводяться —
  таблиць немає, міграцію за рішенням 7 зараз не додаємо.
- Виклики в 11 точках ще **не перемкнені** на сервіс. Перемкнено лише спільну
  утиліту, якою користуються Orders і UI. Решта маршрутів переводиться окремо,
  бо кожен потребує звірки формату відповіді свого споживача.
- Аномальне архівне замовлення `ready_for_issue` (order 7970, 55 од.) канонічною
  формулою більше не резервує. Це **не приховане виправлення даних**: сам запис
  лишається невідповідністю й потребує окремого рішення власника.

---

## 6. Що НЕ змінювалося

Фаза 1 (інвентаризація) не змінювала нічого. У фазі 2 змінено рівно два файли
бізнес-коду (`utils/availability_checker.py` як фасад і новий пакет
`services/availability/`), додано тести. Жодного DDL. Жодного контакту з живою
production-БД — усі виміри зроблені на локально відновленому знімку.